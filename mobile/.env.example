# LineSight Mobile API

Copy values into your shell or a local `.env` (not committed).

```bash
# Physical device — use your machine LAN IP
EXPO_PUBLIC_API_URL=http://192.168.1.20:4000/api
```

Simulators use built-in defaults (localhost / 10.0.2.2).
